# Timetable helper (WIP)

Goal: ask "今天有什么课" and get a list of classes (periods, name, room) filtered by teaching week ranges.

Data file: `schedule.json`

CLI:

```bash
python3 schedule.py today
python3 schedule.py 2026-03-04
```
